django==4.2.7
mongoengine==0.27.0
pymongo==3.12.3
twilio==8.10.0
openai==1.12.0
python-dotenv==1.0.0
colorama==0.4.6
tabulate==0.9.0
groq>=0.4.0

# Async task queue + Redis session cache
celery[redis]>=5.3.0
redis>=5.0.0

# Vector knowledge base
chromadb>=0.4.0
sentence-transformers>=2.4.0
# torch CPU-only — avoids pulling in CUDA (~3 GB) on cloud/server installs:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# If you already have a GPU build of torch installed, remove the line above.
numpy>=1.26.4
